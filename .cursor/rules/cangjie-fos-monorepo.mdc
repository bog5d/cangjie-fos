---
description: CangJie_FOS 单体仓库结构、模块化红线、测试与多租户隐私
alwaysApply: true
---

# CangJie_FOS — Monorepo 与工程红线

## 目录与交付

- 根目录：`CangJie_FOS`。后端：`backend/`（FastAPI + LangGraph）；前端：`frontend/`（React + Vite）。
- 前端产物：`frontend` 构建输出 `dist/`，由 **FastAPI 静态挂载** 伺服；提供 **一键启动脚本**（根目录脚本为准）。
- 详细产品/架构总纲见：`docs/MASTER_PRD.md`。

## 模块化（禁止上帝文件）

- 禁止单文件承载路由 + 业务 + 数据访问 + 提示词等全栈逻辑。
- 新代码按职责拆分，优先落在：`router` / `service` / `schema` / `utils` / `event`（或项目内等价命名），保持单文件职责单一、可测试。

## 测试基线

- 迁移或重构旧功能必须 **同步迁移或新增测试**，不得降低覆盖意图。
- `backend/tests/`：**620+ 条通过**为合并前的质量基线（若仓库尚未达标，以「只增不减」为默认策略，直至达标）。

## 多租户与隐私

- 子智能体与业务数据 **强绑定 `tenant_id`**（查询、缓存、日志中的业务载荷均须带租户边界）。
- **禁止**敏感明文或可归因业务细节在租户之间流动（含共享 prompt 上下文、错误上报 payload、跨租户批量任务）。

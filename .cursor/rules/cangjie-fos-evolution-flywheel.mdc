---
description: CangJie_FOS 自我进化飞轮（捕获→反思→校验→固化）实现约束
alwaysApply: true
---

# CangJie_FOS — 自我进化飞轮 (Self-Evolution)

实现或改动与「学习/迭代」相关的功能时，必须对齐四段流水线，**不得跳过校验直接写生产 Prompt/Skill**。

## 1. 捕获层 (Capture)

- 持久化 **AI 输出 vs 用户手工修改** 的 **Diff**（含元数据：任务类型、`tenant_id`、时间、版本号）。
- Diff 是反思的输入；捕获路径须可异步、可重试。

## 2. 反思层 (Reflection)

- **异步**消费失败/修正样本，产出结构化增量：投资人偏好、业务约束、话术/逻辑补丁等。
- 输出须可审计（可追溯样本 id），避免匿名黑盒覆盖。

## 3. 校验层 (Red Teaming)

- 合并到「生产可用」配置前，必须经过 **模拟苛刻投资人** 的对练/对抗测试。
- 未通过校验的候选规则：**不得**进入子智能体默认路径。

## 4. 固化层 (Merge)

- 仅校验通过后，将候选合并进 **子智能体核心 Prompt / Skill 库**（建议版本化 + 可回滚）。
- 合并操作与租户边界一致：默认 **按 `tenant_id` 隔离** 存储与生效范围，禁止跨租户泄漏。

## 参考

- 产品世界观与完整规约：`docs/MASTER_PRD.md`。

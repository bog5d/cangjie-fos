---
description: 发版/PR 前质量门禁；与 ci_check、外发纯净包说明对齐
alwaysApply: true
---

# 发版与 PR 前质量门禁

在宣称「可合入」「可发版」「可对外交付」涉及本仓库的改动时，应遵守下列与自动化脚本一致的约束。

## 1. 运行 `ci_check`

- **在变更较大或准备合入/打外发包前**，应在 **CangJie_FOS 根目录** 执行 `& .\tools\ci_check.ps1`（依赖本机已安装 `uv` 与 Node；失败则非零退出）。
- 在对话或 PR 说明中，应**如实**写出结果：**已通过**或**未通过及失败项**；不得虚构已绿。
- 命令行与背景说明见 `docs/RELEASE_CHECKLIST.md` 第 5 节与 `AGENTS.md`。

## 2. 与外发/纯净包的关系

- 质量门禁**不**替代**真机打 zip、SHA256 报告、英文路径自测**（那套仍见 `cangjie-fos-external-release.mdc`）。
- 对外默认使用 `build_release_zip.ps1` 的 **`-Profile Release`（默认）**；勿把软著、历史交付、无关大素材树默认打进外发包。见 `docs/RELEASE_CHECKLIST.md` 第 0 节「纯净外发包」。

## 3. 与「不得假称已出包」同逻辑

- 与「未在磁盘上生成 .zip 却称外发已就绪」相同：若**未**运行或**未**通过 `ci_check`，**不得**称「质量门禁已满足」或含糊表述为已测过。

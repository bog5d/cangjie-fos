# 仓颉 FOS — 同事安装上手指南

> 写给：第一次拿到这套系统的同事  
> 前提：你不需要懂代码，只要会双击和复制粘贴  
> 预计时间：首次约 15~30 分钟（主要是等下载）  
> **当前版本：0.6.9（2026-05-15）**

---

## 一、准备工作：先找王波要这一样东西

- [ ] `CangJie_FOS_Release_xxxxxxxx.zip` 压缩包

> **v0.6.9 开箱即用**：API Key 和登录账号已内置在包里，不需要另外填写。直接解压启动即可。
> 登录账号：`zt001` / 密码：`123456`

---

## 二、安装步骤（首次，约 15~30 分钟）

### 第一步：检查 Python 是否已安装

1. `Win + R` → 输入 `cmd` → 回车
2. 输入 `python --version` → 回车
3. 显示 `Python 3.11.x` 或更高 → ✅ 跳下一步
4. 没有或版本太旧 → 去 [python.org/downloads](https://www.python.org/downloads/) 下载，**安装时必须勾选"Add Python to PATH"**

---

### 第二步：解压到正确位置

**⚠️ 不能解压到中文路径（如桌面、下载文件夹）**

推荐：在 C 盘根目录建 `FOS` 文件夹，解压到 `C:\FOS\`

解压后目录结构应如下（AI_Pitch_Coach 由王波单独提供）：

```
C:\FOS\
  ├── AI_Pitch_Coach\       ← 必须，含 src\ 子目录
  ├── CangJie_FOS\
  │     ├── 点击开始-仓颉FOS.bat
  │     ├── 填写API密钥_双击我.bat
  │     ├── 诊断_打不开请运行我.bat
  │     ├── backend\
  │     └── frontend\
  └── .fos_data\            ← 可选，资产台账扫描后自动生成
```

---

### 第三步：填写 API Key

双击 **`填写API密钥_双击我.bat`** → 记事本自动弹出 → 填入 Key → `Ctrl+S` 保存 → 关记事本

```
DEEPSEEK_API_KEY=sk-你的key粘贴在这里
DASHSCOPE_API_KEY=sk-你的key粘贴在这里（ASR转写用，必填）
FOS_ACCOUNTS=账号:密码:账号（找王波要，例如：zt001:123456:zt001）
```

> Key 和账号只填一次，以后直接启动。  
> `FOS_ACCOUNTS` 中，第三个字段（冒号后的账号）决定你的数据分区，与第一个字段保持相同即可。

---

### 第四步：启动

双击 **`点击开始-仓颉FOS.bat`**

- 首次启动会下载 Python 依赖（约5~15分钟，窗口安静是正常的）
- 看到 `✅ CangJie FOS 已启动 → http://localhost:8000` 就成功了
- **浏览器会自动打开，黑色窗口不要关**

---

## 三、系统现有功能一览（0.6.0 版）

### 🔑 登录（v0.5.0 新增）

启动后浏览器显示**登录页**，输入王波给你的账号密码，点「登录」。  
登录后主页顶部显示「👤 你的账号名」即表示成功。

---

### 🎯 路演分析（v0.5.0 全新，推荐先测这个）

**入口：** 主页顶部「**🎯 路演分析**」紫色按钮（在「复盘上传向导」右边）

独立的5步向导，专为路演分析优化：

| 步骤 | 做什么 |
|------|--------|
| 步骤1 | 上传录音（mp3/m4a/wav）**或直接粘贴文字稿**（推荐） |
| 步骤2 | 等待ASR转写进度（文字稿直接跳过此步） |
| 步骤3 | **说话人身份确认** — AI预推测每人角色，你填写真实姓名 |
| 步骤4 | AI深度分析进行中（约30~60秒） |
| 步骤5 | 路演情报报告全文 |

**核心优势**：AI分析时已知道「说话人0是某VC合伙人」，分析质量比旧版高很多。

---

### 🎙 路演录音复盘（核心功能）

**入口：** 主页右上角「**复盘上传向导**」按钮

| 场景 | 操作方式 | 适合谁用 |
|------|----------|---------|
| 有录音文件 | 选择 `.m4a .mp3 .mp4 .wav`，上传分析 | 录音复盘 |
| 有会议文字稿 | 选择 `.txt` 文件，直接上传（跳过转写，更快） | 速记/纪要 |

**分析结果有两种报告，取决于场景选择：**

| 场景选择 | 报告类型 | 看什么 |
|---------|---------|--------|
| `01_机构路演` | 🔍 **路演情报报告** | 对方关键问题、兴趣信号、隐性顾虑、下一步行动 |
| 其他场景（尽调/高管访谈等） | 📊 **风险评分报告** | 总分、风险点列表、改进建议 |

---

### 🔍 路演情报报告（0.4.1 新增）

选择场景 `01_机构路演` 后，AI 不再给你打分，而是像老分析师一样解读整场会议：

**报告包含：**
- **会议氛围**（🔥 高度积极 / ✅ 正常推进 / ❄️ 兴趣不足）+ 阶段判断（初次/深度沟通/准尽调）
- **对方关键问题**：每个问题 + 背后真实关切（核心/关注/礼节三级）
- **兴趣信号**：正面/疑虑/中性，每条附解读
- **隐性顾虑**：对方没说出口但可能担心的
- **关键原声**：最重要的几句原话（值得存档）
- **机构档案更新建议**：AI 基于本次路演建议更新机构画像的内容
- **下一步行动**：谁（我方/对方）、做什么、紧急程度、是否已承诺

---

### 📋 待跟进行动项（0.4.1 新增）

路演情报分析完成后，报告里的「下一步行动」会**自动落盘**，出现在主页的「**待跟进行动项**」清单里。

**在哪里看：** 主页，任务列表下方，点击展开

**怎么用：**
1. 展开清单，看到所有未完成的行动项
2. 确认一项时，点「**完成 ✓**」按钮，该条从清单消失
3. 承诺项（对方/我方已说好的）用橙色标注，建议项用灰色

> 路演回来当天处理完行动项，下次开会前自然不会漏事。

---

### 🏛 机构档案与路演时间线（0.4.1 新增）

**在哪里看：** 主页 → 资料库 → 机构档案 → 点击某机构名 → 侧边栏

侧边栏新增**路演时间线**区块，按时间倒序展示该机构所有关联的路演记录：

- 日期、分析类型（路演/尽调/高管访谈）、状态、路演标题
- 点击任意一条可直接跳转到对应审查台查看详细报告

> 下次见 XX 基金前，先点开他们的档案看一眼历次路演时间线，掌握跟进节奏。

---

### 🤖 豆豆 AI 助手

主页右侧对话框，直接打字问：

- "刚才那场路演对方最担心什么？"
- "见新川基金要准备哪些材料？"
- "我们最近几场路演最常被问到什么问题？"
- "待跟进行动里最紧急的是什么？"

---

### 📁 资料库与匹配

主页下方「资料库」区：

- 上传素材（BP/财务模型/产品文档等）后，系统自动建立索引
- 「智能配对」按钮：输入机构名，自动推荐最匹配的材料组合
- 历次发送记录在「机构档案」里存档

---

### ✏️ v0.6.0 新增改进（2026-05-15）

**复盘审查台**
- **「问题简述」字段**：新增风险点时，除了改进建议，现在还可以填写一句话概括「这个风险点是什么」（30字内）
- **解锁编辑**：报告锁定后，顶部工具栏出现「🔓 解锁编辑」按钮，点击即可恢复编辑状态
- **口述实录**：每个风险点卡片显示原始转写文本，非锁定状态下可直接编辑（纠正 ASR 识别错字或语序问题）

**Pipeline 看板**
- **机构卡片可点击**：点击任意机构卡片弹出编辑窗口，可填写综合画像、核心疑虑、投资偏好
- **Pipeline 阶段可手动切换**：编辑弹窗中有「Pipeline阶段」下拉菜单（触达/路演/尽调/TS），直接改不需要上传录音
- **空内容提示**：空卡片显示「暂无摘要 · 点击编辑机构画像」提示，不再只有个名字

**路演情报报告**
- **编辑摘要入口**：报告顶部新增「✏️ 编辑摘要」按钮，可修改会议氛围综述、隐性顾虑、机构档案更新建议，保存后即生效

---

## 四、怎么测试——逐功能验收清单

拿到新版本后，按这个顺序验收，每项勾掉才算通过：

### ✅ 基础启动和登录（必测）

- [ ] 双击 `.bat` 启动，15秒内浏览器自动打开，显示**登录页**（不是主页）
- [ ] 输入王波给你的账号密码，点「登录」→ 进入主页，顶部显示「👤 账号名」
- [ ] 主页正常显示，没有红色报错框
- [ ] 点「🔧 系统诊断」，至少 6 项绿色 ✅，`ffmpeg` 和 `db` 必须绿

---

### ✅ 路演分析——文字稿模式（v0.5.0 核心新功能）

**最快测试方法，约1分钟出结果：**

1. 点主页顶部「**🎯 路演分析**」紫色按钮
2. 选「📝 粘贴文字稿」标签
3. 复制下面这段文字粘贴进去：
   ```
   说话人A：你们的退出路径是什么？
   说话人A：现在ARR大概多少？IRR怎么算？基金规模多大？
   说话人B：我们的产品核心壁垒在于AI技术，客户已覆盖500强企业。
   说话人B：商业模式是SaaS订阅制，月ARR约50万，今年目标600万。
   说话人A：你们投资逻辑是什么？我们主要看这个赛道的回报。
   说话人B：我们计划3年内科创板上市。
   ```
4. 路演日期选今天，点「**开始分析 →**」

**步骤3（说话人确认）预期：**
- [ ] 出现2个说话人卡片，AI已推测：A → **GP执行**，B → **企业方创始人**
- [ ] 每个卡片显示「样本台词」（AI推测依据）
- [ ] 填入姓名（随便填），点「**确认身份，开始AI分析 →**」

**步骤5（报告）预期（约60秒后）：**
- [ ] 显示会议温度标签（🔥/☀️/🧊 之一）+ 会议阶段
- [ ] 「核心关注点」列表有至少2条，每条显示原话（加引号）和深层顾虑
- [ ] 「兴趣信号」至少1条，显示 ✓ 或 ✗ 图标 + 原话 + AI解读
- [ ] 「约定行动项」至少有1条（显示负责方，无DDL字段）

---

### ✅ 路演情报报告（0.4.1 核心新功能）

**测试方法：用文字稿（.txt）上传，最快，不需要等转写**

1. 新建一个文本文件（比如 `test_meeting.txt`），写入：

   ```
   说话人A: 你们的退出路径是什么？
   说话人B: 我们计划3年内科创板上市，目前已经开始辅导期准备。
   说话人A: 现在ARR大概多少？
   说话人B: 目前月ARR约50万，今年目标600万。
   说话人A: 管理层有没有过往成功退出的经历？
   说话人B: CEO有一次并购退出记录，CFO是IPO老兵。
   ```

2. 点「复盘上传向导」
3. 场景选 **「01_机构路演」**
4. 被访人填"新川基金——测试"
5. 上传刚才的 `.txt` 文件
6. 提交

**预期结果（约 30~60 秒，文字稿无需转写）：**
- [ ] 任务列表出现"评估中"→"完成"，全程不超过2分钟
- [ ] 点「查看报告」，进入审查台，顶部显示「路演情报报告」标题
- [ ] 报告里有「会议氛围」（hot/warm/cold 之一）和「对方关键问题」
- [ ] 报告里出现至少1条「下一步行动」

**⚠️ 常见问题：**
- 审查台显示「数据异常」→ 重启后端再试一次
- 一直转圈不完成 → `DEEPSEEK_API_KEY` 未填或填错

---

### ✅ 待跟进行动项（0.4.1 新增）

*接续上一个测试（路演情报报告已分析完成）*

- [ ] 返回主页，向下滚动，看到「**待跟进行动项**」区块（橙色边框）
- [ ] 点击展开，出现至少 1 条行动项（来自刚才的路演分析）
- [ ] 点「完成 ✓」按钮 → 该条从清单消失
- [ ] 再次刷新页面 → 已完成的项不再出现

---

### ✅ 参与人确认 → 机构绑定（0.4.1 新增）

*接续上一个测试（任务完成后会自动弹出确认弹层）*

1. 任务完成后，若弹出「参与人身份确认」弹层：
   - 给说话人A填：机构"新川基金"，角色"LP投资方"
   - 点「确认」

**预期：**
- [ ] 弹层关闭，无报错
- [ ] 主页「待跟进行动项」里该路演的行动项，来源机构显示「新川基金」

---

### ✅ 机构档案路演时间线（0.4.1 新增）

*接续上一个测试（参与人已确认绑定新川基金）*

1. 主页最下方找到「资料库」→「机构档案」区块（需有历史打包记录才显示）
2. **或者：** 直接打开浏览器访问 `http://localhost:8000` 并在「机构档案」里找"新川基金"

> **注意：** 机构档案卡片只显示「有打包记录」的机构。若"新川基金"之前没有打包记录，可先在「智能配对」里给它配一次。

- [ ] 点击机构名，侧边栏打开
- [ ] 侧边栏出现「**路演时间线**」区块（日期 + 路演/尽调等标签）
- [ ] 点击时间线里的某条 → 正确跳转到对应审查台

---

### ✅ 普通路演评分报告（回归测试，确认没有被影响）

1. 上传同一个 `.txt` 文件
2. 场景选 **非** `01_机构路演`（例如"02_高管访谈"）
3. 提交后查看报告

- [ ] 进入审查台后，页面正常显示「总分」和「风险点」（不是路演情报格式）
- [ ] 风险点可以手动编辑、修改分值

---

### ✅ 豆豆对话（回归测试）

- [ ] 主页右侧豆豆输入框输入「你好」→ 有正常回复
- [ ] 输入「刚才那场路演对方最担心什么」→ 能基于报告内容回答（不是胡说）

---

### ✅ v0.6.0 新功能验收

**复盘审查台**
- [ ] 打开一个已完成的复盘，点「新增遗漏痛点」→ 表单里出现「问题简述」输入框
- [ ] 锁定报告后，顶部出现「🔓 解锁编辑」按钮，点击后可继续编辑
- [ ] 风险点卡片中可看到「口述实录」区块（非锁定状态下文字可编辑）

**Pipeline 看板**
- [ ] 点击任意机构卡片 → 弹出编辑窗口（有综合画像/疑虑/偏好/阶段/热度字段）
- [ ] 填写综合画像并保存 → 卡片内容立刻更新，不需要刷新页面
- [ ] 通过编辑窗口将阶段改为「尽调」→ 卡片阶段标签随即变化

**路演情报报告**
- [ ] 打开一个路演分析结果 → 报告顶部有「✏️ 编辑摘要」按钮
- [ ] 点击进入编辑模式，修改会议氛围综述内容，点保存 → 修改生效，再次打开看到修改后的版本

---

## 五、常见问题

| 问题现象 | 解决方法 |
|---------|---------|
| 登录失败/账号不对 | 联系王波确认账号密码；账号区分大小写；检查 `.env` 里 `FOS_ACCOUNTS` 格式 |
| 路演分析步骤3没有说话人 | 文字稿要有「说话人A:」前缀；无标记文本也可（全归说话人0） |
| 路演分析卡在步骤4不动 | 检查 `DEEPSEEK_API_KEY` 是否填写（右上角 ⚙️ 可测试） |
| Windows 弹出安全警告 | 点「更多信息」→「仍要运行」 |
| 黑色窗口一闪就消失 | 右键 `.bat` → 以管理员身份运行 |
| 首次启动卡在下载 | 耐心等 10~15 分钟，或切手机热点重试 |
| 浏览器没自动打开 | 手动输入 `localhost:8000` |
| 路演情报报告一直转圈 | 检查 `DEEPSEEK_API_KEY` 是否正确 |
| 录音上传后卡在转写 | 检查 `DASHSCOPE_API_KEY` 是否填写 |
| 审查台显示「数据异常」 | 重启后端，再次点「查看报告」 |
| 待跟进行动项不出现 | 场景需选 `01_机构路演`，且报告里需有 next_actions |
| 机构时间线没出现 | 参与人确认时需填机构名；或该机构暂无打包记录 |

---

## 六、实在搞不定

双击运行：**`诊断_打不开请运行我.bat`**

自动检查 9 项（Python/依赖/端口/FFmpeg/数据库等），给出修复建议。  
看不懂就截图发给王波。

---

---

## 五、已知问题与修复记录

### v0.6.0（2026-05-15）修复
- **新增风险点可填「问题简述」**：以前只有改进建议和扣分原因，现在新增了一句话概括字段。
- **锁定报告后可解锁重新编辑**：以前报告一旦锁定就无法修改，现在顶部有「🔓 解锁编辑」按钮。
- **口述实录可编辑**：每个风险点卡片现在显示原始转写文本，可直接编辑纠正 ASR 错字或语序问题。
- **Pipeline 机构卡片可点击编辑**：以前卡片只能看，现在点击弹出编辑窗口，可填写综合画像/疑虑/偏好。
- **Pipeline 阶段可手动切换**：不再需要上传录音才能推进阶段，编辑弹窗中直接改下拉菜单即可。
- **空卡片有提示**：Pipeline 看板中内容为空的卡片现在显示提示文字，不再只有个机构名。
- **路演情报报告有编辑入口**：报告顶部新增「✏️ 编辑摘要」按钮，可修改会议氛围综述、隐性顾虑等字段。

### v0.5.4（2026-05-14）修复
- **路演报告第5步字段空白/undefined**：完成路演分析后，第5步报告里「核心关注点」「兴趣信号」「关键原话」等板块全部显示空白或"说话人undefined"。已在 v0.5.4 修复。
- **删除风险点后总分不变**：在审查台手动删除某条风险点，顶部总分没有自动下调。已修复。
- **历史记录看不到机构名**：复盘历史列表只显示时间和状态，无法区分是哪个机构的路演。已修复，现在显示 🏢 机构名。

### v0.5.3（2026-05-12）修复
- **Chrome浏览器点击无效**：登录后整页被透明薄膜覆盖，任何按钮都点不了。
  已在 v0.5.3 完全修复，**请更新到最新版本**。
- **路演分析完成后左侧大屏没更新**：战情室机构卡片不出现。
  v0.5.3 已打通，路演完成后机构自动出现在「已路演(Pitched)」阶段。

---

*版本：0.6.0（2026-05-15）| 如有问题找王波*

# CangJie_FOS Phase 3 施工任务单 (TODO_LIST)

【执行协议：全自动循环模式】
1. 读取任务 -> 2. 编码实现 -> 3. 运行 `pytest` 及前端联调测试 -> 4. 成功则标记 [x] 并自动下一项。

- [x] **任务 3.1：** 在后端完善 `dashboards` 路由，提供返回漏斗进度和健康度的 API（暂可用 Mock 数据，但必须确定 JSON 契约），前端 Axios 接入。
- [x] **任务 3.2：** 实现前端对话框与后端 LangGraph 的通信。前端发送消息，后端通过 Invoke 图状态机处理并返回，打通文本交互全链路。
- [x] **任务 3.3：** 替换前端的「音频上传」占位符，实现真实的文件 FormData 提交。后端接收并流转至已模块化的音频处理管道。
- [x] **任务 3.4：** 前后端打通「经验值（Exp）」事件总线。例如：当后端成功解析一份录音或落盘一条错题本后，返回特定标识，前端监听到后播放 Exp 增加的动画。
- [x] **任务 3.5：** 在前端集成 Feedback 机制，允许用户对 AI 的气泡消息进行「纠错/修改」，调用 `feedback.py` 中定义的接口，触发后台捕获。
- [x] **任务 3.6：** 全局回归：运行 `pytest` 确保后端全绿，运行 `npm run build` 确保前端构建无误。
